/** Slim footer bar — brand signature across surfaces. */
export default function Footer() {
  return (
    <footer className="shrink-0 border-t border-border bg-surface px-4 py-2 text-center">
      <span className="font-mono text-[12px] tracking-[0.14em] text-text-faint">YoDev</span>
    </footer>
  );
}
